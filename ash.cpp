#include <iostream>
#include <string.h>
using namespace std;

int main()
{
    string s;
    getline(cin, s);
    cout<<s.length();
    for(int i=0;i<s.length();i++)
    cout<<int(s.at(i))<<"\t";
}
/*int hash(char arr[][],int m){
    for
        for
            sum+=int)arr[][]

}
*/
