#include<iostream>
using namespace std;

int sums(int a, int b)
    {   float para;
        para = (a+b)/2;
        int sum = a+b;
        return sum,para;
    }
int main()
{   int sums(int a, int b);
    int l,m;
    cin>>l>>m;
    cout<<sums(l,m);
    return 0;
}
